RUN.SH: shell script to run both programs and describe what is happening.

prog1.c: Copy files (binary and text) using functions, compile, debug, run, and test

prog2.c: Copy files (binary and text) using system calls, compile, debug, run, and test

Both programs integrate third task, calculating time

src1.dat, src2,datm=, 2 source files of different sizes.

Used AI to help write shell script to run programs, did not copy code.
